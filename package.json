{
  "name": "rtcbun",
  "version": "0.1.0",
  "scripts": {
    "build": "./node_modules/.bin/esbuild --bundle ./src/stylesheet.css --outfile=./dist/bundle.css && ./build.js",
    "start": "cross-env envi=production bun run ./src/server/server.js",
    "startdev": "cross-env envi=development bun run ./src/server/server.js"
  },
  "dependencies": {
    "path": "^0.12.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "web-vitals": "^3.0.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.17.10",
    "react-refresh": "0.10.0",
    "typescript": "latest"
  }
}